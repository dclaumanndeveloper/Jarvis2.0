# ============================================
# Jarvis 2.0 - Dependencies (requirements.txt)
# ============================================
# NOTE: Python 3.14 requires special handling for some packages.
# Run: pip install pipwin && pipwin install pyaudio
# Then: pip install -r requirements.txt
# ============================================

# === Audio & Speech ===
# PyAudio - Install separately: pipwin install pyaudio
SpeechRecognition>=3.10.0         # Speech-to-text (Google API)
pyttsx3>=2.90                     # Text-to-speech engine
sounddevice>=0.4.6                # Alternative audio I/O (fallback for PyAudio)
librosa>=0.10.0                   # Audio processing & MFCC extraction
audioread>=3.0.0                  # Audio file reading (librosa dependency)
numpy>=1.24.0                     # Numerical operations

# === Windows Audio Control ===
pycaw>=20240210; sys_platform == 'win32' # Windows Core Audio API (volume control)
comtypes>=1.4.0; sys_platform == 'win32' # COM interface (Windows)
pywin32>=310; sys_platform == 'win32'    # Windows extensions

# === GUI ===
PyQt6>=6.5.0                      # Modern Qt6 GUI framework

# === AI & NLP ===
google-generativeai>=0.3.0        # Gemini API
python-dotenv>=1.0.0              # Environment variables (.env support)
scikit-learn>=1.3.0               # Machine Learning
joblib>=1.3.0                     # Model serialization

# === Web & Browser ===
requests>=2.28.0                  # HTTP requests
beautifulsoup4>=4.12.0            # HTML parsing
pywhatkit>=5.4                    # WhatsApp, YouTube integration
wikipedia>=1.4.0                  # Wikipedia API

# === Automation ===
PyAutoGUI>=0.9.54                 # Mouse/keyboard automation
pyperclip>=1.8.0                  # Clipboard operations
# pillow - Install separately if needed: pip install pillow --only-binary :all:

# === System Monitoring ===
psutil>=5.9.0                     # System info (CPU, memory, disk)
speedtest-cli>=2.1.3              # Internet speed test

# === Utilities ===
geocoder>=1.38.1                  # Geolocation (weather API)
tzlocal>=5.0                      # Timezone handling
colorama>=0.4.6                   # Colored terminal output

# === Data Persistence ===
aiosqlite>=0.19.0                 # Async SQLite for learning engine
aiofiles>=23.2.1                  # Async file I/O

# === Standard Library Backports (Python 3.13+) ===
audioop-lts>=0.2.1; python_version >= '3.13' # Python 3.13+ compatibility
