name: Build Windows Installer

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v3
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build .exe with PyInstaller
        run: |
          pyinstaller --onefile seu_script_principal.py

      - name: Criar instalador (opcional)
        run: |
          # Exemplo: usar Inno Setup, NSIS ou outro criador de instalador
          echo "Adicione aqui comandos para criar o instalador .exe"

      - name: Instalar como serviço do Windows (opcional)
        run: |
          # Exemplo simples usando NSSM (https://nssm.cc/)
          # Baixe NSSM e instale o serviço apontando para seu .exe gerado
          echo "Adicione aqui comandos para instalar como serviço do Windows"
